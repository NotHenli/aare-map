{
  "name": "aare-map",
  "version": "0.2.0",
  "private": true,
  "description": "Aare float safety map Thun → Bern",
  "scripts": {
    "dev": "npx serve -l 3000 .",
    "check": "node scripts/check.js",
    "build": "npm run check && node scripts/build.js",
    "test": "npm run build"
  },
  "devDependencies": {
    "sharp": "^0.34.2"
  }
}
